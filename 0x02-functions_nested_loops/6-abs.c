#include "main.h"

/**
 * _abs - print the absolute value of an integer
 * @n: integer to check
 * Return:positive  integer value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
